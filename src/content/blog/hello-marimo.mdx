---
title: Hello, marimo
date: 2026-06-18
description: A first interactive post — proving inline marimo islands end to end.
tags: [marimo, astro]
interactive: true
---

import MarimoCells from '../../components/MarimoCells.astro';

This site embeds [marimo](https://marimo.io) notebooks directly in the page. Not
a screenshot, not a video — the actual reactive runtime, running in your browser.

Move the slider and the squares recompute — Python running in your browser with
no server round-trip.

<MarimoCells slug="hello-marimo" cells={[0, 1, 2]} runtime />

Reactivity is the whole point. A marimo notebook is a dependency graph of cells,
so changing one input propagates only to the cells that depend on it — the same
idea that makes spreadsheets feel alive, applied to Python.

The two blocks here are cells of one notebook, placed at different spots on the
page. Below, the same slider drives an [Altair](https://altair-viz.github.io)
chart of `x²` — pandas and Vega-Lite, all client-side.

<MarimoCells slug="hello-marimo" cells={[3, 4]} />
